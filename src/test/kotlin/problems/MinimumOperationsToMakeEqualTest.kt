package problems

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals

class MinimumOperationsToMakeEqualTest {
    private val solution = MinimumOperationsToMakeEqual()

   /* @Test
    fun `test case 1 - example from problem`() {
        val nums = intArrayOf(5,2,5,4,5)
        val k = 2
        assertEquals(2, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 2 - impossible case`() {
        val nums = intArrayOf(2,1,2)
        val k = 2
        assertEquals(-1, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 3 - decreasing sequence`() {
        val nums = intArrayOf(9,7,5,3)
        val k = 1
        assertEquals(4, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 4 - already equal`() {
        val nums = intArrayOf(3,3,3,3)
        val k = 3
        assertEquals(0, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 5 - single element`() {
        val nums = intArrayOf(5)
        val k = 2
        assertEquals(1, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 6 - all elements less than k`() {
        val nums = intArrayOf(1,2,3)
        val k = 4
        assertEquals(-1, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 7 - mixed values`() {
        val nums = intArrayOf(10,5,10,8,10,6,4)
        val k = 3
        assertEquals(4, solution.minOperations(nums, k))
    }

    @Test
    fun `test case 8 - k equals max value`() {
        val nums = intArrayOf(5,8,5,8,5)
        val k = 8
        assertEquals(3, solution.minOperations(nums, k))
    }*/
}
